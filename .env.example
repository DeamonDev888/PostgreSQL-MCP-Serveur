# Financial Analyst - Environment Configuration
# Enable database
USE_DATABASE=true

# PostgreSQL Configuration (compatible avec Financial Analyst)
POSTGRES_HOST=localhost
POSTGRES_PORT=
POSTGRES_DATABASE=
POSTGRES_USER=
POSTGRES_PASSWORD=

# Alternative: Use full connection string

# Additional PostgreSQL Settings
POSTGRES_SSL=false
POSTGRES_MAX_CONNECTIONS=10
POSTGRES_IDLE_TIMEOUT=30000

# Environment
NODE_ENV=development

# Optional: Connection String (overrides individual settings)

# News Scraping Configuration (placeholder pour future implémentation)
# NEWS_API_KEY=your_api_key
# NEWS_SOURCES=source1,source2